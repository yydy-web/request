# This workflow will run tests using node and then publish a package to GitHub Packages when a release is created
# For more information see: https://docs.github.com/en/actions/publishing-packages/publishing-nodejs-packages

name: Node.js Package

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    permissions:
      id-token: write
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Set node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          registry-url: 'https://registry.npmjs.org'

      # Trusted publishing (OIDC) requires npm CLI >= 11.5.1.
      - name: Update npm
        run: npm install -g npm@latest

      - run: npx changelogithub
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}

      - name: Install Dependencies
        run: pnpm i

      - name: PNPM build
        run: pnpm run build

      # Build with pnpm, but publish the single package with npm so it can use
      # OIDC trusted publishing. No NPM_TOKEN needed.
      # Prerelease versions (e.g. 2.0.0-beta.1) must go under a dedicated
      # dist-tag; only stable versions are published to `latest`.
      - name: Publish to NPM
        working-directory: packages/request
        run: |
          echo "node $(node -v) / npm $(npm -v)"
          VERSION="$(node -p "require('./package.json').version")"
          if [[ "$VERSION" == *-* ]]; then
            TAG="${VERSION#*-}"
            TAG="${TAG%%.*}"
            [ -z "$TAG" ] && TAG="next"
            echo "Publishing prerelease $VERSION under dist-tag '$TAG'"
            npm publish --provenance --access public --tag "$TAG"
          else
            echo "Publishing stable $VERSION under dist-tag 'latest'"
            npm publish --provenance --access public
          fi

  # Build and deploy the docs site to GitHub Pages after a successful release.
  # Kept as a separate job so its `environment` does NOT leak into the OIDC
  # claims used by the npm trusted-publishing job above.
  deploy-docs:
    needs: release
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Set node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install Dependencies
        run: pnpm i

      - name: Build docs
        run: pnpm -F docs build

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload docs artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: packages/docs/doc_build

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
